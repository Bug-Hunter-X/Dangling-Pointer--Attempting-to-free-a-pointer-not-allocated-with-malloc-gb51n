int main() {    int x = 10;    int *ptr = &x;    *ptr = 20;    printf("%d\n", x); // Output: 20    free(ptr); //ERROR:  Attempting to free a pointer that was not allocated using malloc, calloc, or similar functions. This leads to undefined behavior. }